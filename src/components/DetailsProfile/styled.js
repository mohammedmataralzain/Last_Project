import styled from "styled-components";

export const StyleDetailsProfile = styled.div`
 max-width: 60rem;
  .top {
    display: flex; 
    justify-content: space-between;
    flex-wrap: wrap; 
    div {
      align-items: center;
    }
  }
 
`;
