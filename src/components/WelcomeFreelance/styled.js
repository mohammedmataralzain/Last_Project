import styled from "styled-components";

export const StyleWelcomeFreelance = styled.div`
  padding: 1.5rem 2rem;
  border: 1px solid var(--border-color);
  border-radius: 12px;
  font-size: 1.9rem;
  border: 0.1rem solid #d5e0d5;
  div:last-child {
    font-size: 2.9rem;
    font-weight: 500;
  }
  margin-bottom: 1.6rem;
`;
