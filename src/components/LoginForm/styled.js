import styled from "styled-components";

export const StyleBoxPassword = styled.div`
position: relative;
.eye_close , .eye_open {
  position: absolute;
  top: 40%;
  transform: translateY(-50%);
  right: 1rem;
}
` 